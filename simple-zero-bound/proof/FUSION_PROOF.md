# PGF-C first complete fusion result

## 1. Result and exact scope

The result is **STRICT_FUSION_GAIN_EXACT**, relative to the explicitly imported analytic and universal local-certificate evidence of the frozen source package and the exact modules of the two frozen specialists. The certified separator is

\[
 s_0=\frac{6734775921119}{10000000000000}=0.6734775921119,
\qquad
 s_0-r_*=
 \frac{34271814394247548721}{491155304728390000000000000}>0.
\]

Consequently the combined argument gives
\(\liminf N_0^{\mathrm s}(T)/N(T)\ge s_0>r_*\).
The strict separator actually excludes limiting states at or below s0; the weaker displayed non-strict bound is sufficient and avoids claiming an unrecorded stronger decimal. This is a research result based on frozen campaign theorem inputs, not a new independent hostile audit or formal verification of those inputs.

The winning finite certificate uses m=492, the 69-row reflected-trim polyhedron, a positive same-Gram tail price, jointly reoptimized six position prices and reward, and the exact G6 scalar consequence of the full G3/G4 residual region. The search preserved the trace-refined quotient and the direct q0,Z pieces; exact interval checks show that their full outer-state optimization agrees with G6 at the selected parameters. The global state and local energy are identified explicitly below. No numerical gains are added.

The bounded search covered every integer 250<=m<=750, with the exact triple/singleton correction at each m and each trim. It also covered the original nine-row family as a supplementary regression. There is no claim that m=492 is a rigorously certified global maximizer, nor any claim about m outside this assigned bounded scan. A positive exact theorem requires a valid separator, not a certified optimum. No global no-gain claim is made.

The two ACTIVE_CONDITIONAL imports are preserved verbatim in MODULE_GRAPH. G5_OFFSET_TRANSPORT is an optional schema; B08 supplies its premise and its identical conclusion for our exact plane. The exact winning proof uses B08 directly and does not depend on that conditional module. G9_CONDITIONAL_MOMENTS remains an uninstantiated conditional class: no d0,qbar is supplied, and neither is treated as a free numerical parameter. No conditional higher-moment premise enters the winning result.

## 2. Four frozen inputs and source authority

The four outer SHA256 values are recorded in INPUT_INTEGRITY_RECEIPT. They were checked before mathematical use. The hashes match the assigned PGF_C_BASE_PACKET.zip, PGF_A_FIRST_FROZEN_OUTPUT.zip, PGF_B_FIRST_FROZEN_OUTPUT.zip and Zeta_Reblocking_Repro.zip. Both specialist manifests, the base packet manifest, and all 57 primary distribution-manifest entries were checked. Only the isolated extraction of those four inputs was read. No live A/B scratch or output directories were read, and no A/B communication occurred.

The original reproduction TeX, candidate JSON, parameters and structural certificates are authoritative. The exact nine pair arrays, six pressures per row, window coefficients, local targets, B, triple target and baseline were compared with the primary data. The Sol transcription agrees in the constants and prices used. The separately supplied PDF differs from the reproduction PDF; it was not substituted for the original TeX. The source window is the 25-term window, not the historical seven-term project window.

The retained FULL_REPLAY.json PASS, with 58,577,037 visited nodes, is imported pinned evidence. C did not run reproduce.py --full, any local interval certification, new all-gap certificates, author assembly/freeze scripts, literature searches, Lean, or public uploads. In particular the new exact verifier proves the finite fusion inequalities and arithmetic; it does not turn the imported universal local evidence into a newly executed certification.

The exact constants are

\[
 h=\frac{210042647916503}{312500000000000},\quad
 B=\frac{1970462189}{500000000000},\quad
 \epsilon=\frac{15729481}{2000000000},\quad
 \eta=\frac{78681}{10000000},\quad\tau=7/6.
\]

The primary fixed-function unweighting and window enclosure are imported analytic premises. Limits are taken with each fixed m,rho,price vector first, height tending to infinity next, and smoothing tending to zero last.

## 3. Common state and one budget

Use A's exact Proposition-3.2 realization: A=P+Q is the signed real operator; V has the s simple-real unit columns; P=VV*, G=V*V; tr A=N and tr G=s. Let beta=rank(Q_+), q0=N-s-2beta>=0, and let k and ell be respectively the number of eigenvalues of this full G above 2 and tr(G-2I)_+. Write

\[
 \Phi(t)=(t-1)^2-(t-2)_+^2,\qquad D=\operatorname{tr}\Phi(G),
 \qquad Y=2q_0+W+J+Z\ge0.
\]

A proves on this exact state

\[
 S_2-2N+s=D+Y,
\quad hN+D+Y\le s+o(N),
\quad Y\ge 2q_0+Z+\frac{(\ell+q_0)^2}{\beta+k}.
\]

The stronger trace refinement replaces the quotient by
\(\inf_{T_H\ge0}[(T_H+q_0)^2/\beta+(\ell-T_H)_+^2/k]\).
All zero denominators have the lower-semicontinuous perspective convention. These are simultaneous restrictions of one residual, never several payments.

On one joint normalized subsequence put

\[
 x=s/N,\quad u=k/N,\quad v=\ell/N,\quad
 w=\beta/N,\quad c=q_0/N=1-x-2w,
 \quad z=Z/N,\quad d=D/N,\quad y=Y/N.
\]

The full exact outer region includes

\[
 h\le x\le1,\quad u,v,w,c,z,d,y\ge0,\quad 2u+v\le x,
 \quad d+y\le x-h,
\]
\[
 d\ge F(x,u,v):=u+2v+\frac{(u+v)^2}{x-u},
 \quad y\ge 2c+z+Q_{w,u}(v,c).
\]

For the relevant x>h, x-u>0. Normalized u=0 does not force v=0. No local or global high-count rank is pinched. The only transported spectral resource is D+rho*ell of the same G.

For any certified local plane the B08 all-offset and fixed-smoothing theorem, on this common state, gives

\[
 d+\rho v\ge \frac{Rx-P_{\rm price}}m,
\qquad P_{\rm price}=B\left(2\sum_{k=0}^4t_k+(m-11)t_5\right).
\]

The single identity used in the endgame is
\(d+y=(d+\rho v)+(y-\rho v)\).
Count credit q0, overlap Z, rank constraints and the tail are already inside these terms. The local band matrix sometimes named Y is entirely different from residual Y; its norm defines the energy below.

## 4. Local family and exact m dependence

For an m-point Gram block use

\[
 E=2\sum_{i<j,\ j-i\le6}|G_{ij}|^2,\qquad n=m-6,
 \quad e=E/n,\quad u_k^{\rm loc}=B S_k/n.
\]

Here S_k is the sum of the two gaps at distances k from the ends for k<5, while S_5 is the sum of all interior gaps. These local u coordinates are not the global high-count u. They are distinguished in code by their separate arrays and are never identified.

For primary row j let d_{j,k}=sum_{r<=k} b_{j,r}, extended by zero for k<0. Define the unnormalized target on v starts by

\[
 T_j(v)=v\epsilon_j\ (j\ne\mathrm{base}),\qquad
 T_{\mathrm{base}}(v)=3\lfloor v/3\rfloor\eta+(v\bmod3)\epsilon.
\]

For every 0<=a<=b<=5 B proves the reflected trim constraint

\[
 e+\sum_{k=0}^4\frac{d_{j,k-a}+d_{j,k-b}}{2B}u_k^{\rm loc}
       +\frac{d_{j,5}}B u_5^{\rm loc}
 \ge\frac{T_j(n-a-b)}n.                 \tag{L}
\]

All 21 base trims are retained; for each of eight other rows only the six symmetric trims are needed, giving 69 canonical inequalities. Each row separately comes from nonnegative copies of the certified instances whose physical pair loads are <=2 and whose spans are <=6. The copies have reflection weight 1/2 or 1. Multiple rows are co-imposed on the same e,u, not summed as separate defects. The full pressure envelope, including its shift cost, is used.

At every m the base untrimmed remainder is (m-6) mod 3; each trimmed row uses its own (m-6-a-b) mod 3. At m=492, n=486 and the untrimmed packet has 162 triples and no singleton. Other trims still have their own singleton corrections. B's 36 exact acyclic Farkas certificates remove c1005/c101 only for floating search in the full trim family, and the exact midpoint criterion removes some base asymmetric rows. The final local verifier retains all 69 canonical rows, so its validity does not rely on those reductions or on the completeness of a computed vertex list.

B's fixed templates expose no additional continuous free multiplier. Their packet weights are not tunable fusion parameters. The dual weights in our proof certificates are nonnegative Farkas combinations for plane certification, not new physical packet capacity.

## 5. Exact plane certificate without a vertex-completeness assumption

B's independently proved increasing concave profile is

\[
 G_{m,\rho}(E)=
 \begin{cases}
 E,& E\le\tau(1+\rho/2)^2,\\
 \displaystyle\frac{E+m(2(1+\rho/2)\sqrt{E/\tau}-(1+\rho/2)^2)}{1+m/\tau},&\text{otherwise}.
 \end{cases}
\]

It lower-bounds D_block+rho*ell_block. C must prove

\[
 G_{m,\rho}(ne)+n\sum_{k=0}^5t_k u_k^{\rm loc}\ge R             \tag{P}
\]

on the whole nonnegative polyhedron (L). Floating polar-hull vertices were used only to discover parameters and energy knots. Their completeness is not a premise of (P).

The certificate lists rational knots 0=E_0<...<E_K and rational lower values y_i<=G(E_i). Each y_i is checked exactly: on the nonlinear branch, set

\[
 q=\frac{(1+m/\tau)y_i-E_i+m(1+\rho/2)^2}{2m(1+\rho/2)}.
\]

If q<=0 the comparison is automatic; otherwise check q^2<=E_i/tau. This is sign-safe squaring. Linear-branch checks use y_i<=E_i directly.

For each adjacent pair draw the affine chord a_i E+b_i through their lower values. Include the final constant ray y_K. Concavity proves that on each corresponding interval its chord is <=G(E). Monotonicity proves y_K<=G(E) for E>=E_K. Thus at every E>=0 at least one listed line lies below G(E). We do not require every chord extension to lie below G everywhere.

For every listed line, its exact rational nonnegative dual weights lambda_j satisfy, coordinatewise,

\[
 \sum_j\lambda_j A_j\le n(a_i,t_0,...,t_5),\qquad
 b_i+\sum_j\lambda_j b_j\ge R,
\]

where A_j z>=b_j are the canonical rows (L). Multiplying by z>=0 and summing proves that **every** listed line plus the pressure is >=R throughout the entire polyhedron. Selecting the line below G at the actual energy proves (P). This covers all unbounded recession directions; no finite energy cap, missing vertex, floating tolerance or unverified hull completeness enters the proof.

The winning file contains 464 such line/ray certificates. All endpoint checks, coefficient inequalities, price costs and endgame comparisons pass with Python Fraction arithmetic. The replay uses no numerical optimizer or interval library. A price/reward proposal becomes a theorem only after these checks.

## 6. Joint global-state minimization and residual alternatives

G2's Young family, G3's exact quotient, trace refinement, G6 and both G8 fallbacks are alternate complete lower bounds for y-rho*v. The full state permits their simultaneous imposition or pointwise maximum, never addition. G3 dominates every same-state Young support by a completed square. The rank projection is implied by the coupled region. The retained-piece conservative B_ret payment is also implied there.

For an explicit scalar elimination drop c,z to zero and increase w to (1-x)/2. This relaxes the residual inequality while preserving spectral F. Put

\[
 a=(1+x)/2,\quad t=2x-h,\quad
 N_x=((2-h)x-h)/2>0,\quad A_x=t+\rho x.
\]

A proves

\[
 y-\rho v\ge-p_h(x;\rho),\qquad
 p_h=\frac{\sqrt{A_x^2+\rho^2N_x}-A_x}2,
 \quad p_h^2+A_xp_h=\rho^2N_x/4.                  \tag{G6}
\]

For completeness the scalar minimization behind this is transparent. With q=u+(1-x)/2 and sigma=v/q, the budget implies

\[
 q(t+2x\sigma+a\sigma^2)\le N_x.
\]

For 0<=sigma<rho, minimizing y-rho*v uses the largest admissible q. The unconstrained stationary point is

\[
 \mu=p_h/N_x,\quad
 \sigma_*=(\rho-2\mu x)/(2(1+\mu a)),\quad
 q_*=N_x/(t+2x\sigma_*+a\sigma_*^2).
\]

If q_* >= (1-x)/2 and the trace constraint holds, set
u=q_*-(1-x)/2, v=q_*sigma_*, y=q_*sigma_*^2, c=z=0, d=F.
Then d+y=x-h, y=v^2/(u+w), and the G6 minimum is attained in the **outer scalar region**. The trace refinement also attains equality with T_H=w*v/(u+w), omega=T_H^2/w and j=(v-T_H)^2/u. Therefore retaining c,z,T_H,omega,j does not improve that scalar minimum at these parameters. This is not a realization by actual zero ordinates.

For the complementary region the constrained minimum is at u=0. The budget root v_max solves
\(2v+(1/x+1/w)v^2=x-h\), and the penalty is rho*v_max-v_max^2/w. The search implements this boundary branch as well. For its actual 1002 scan proposals and 14 exact full-route certificates, rational interval evaluation proves the interior q_*>w and trace-domain tests; therefore G6 and the complete refined G3/G4 state agree there. STATE_ELIMINATION_RECEIPT records each check.

The following weaker routes were independently price/rho-reoptimized and exact-certified at m=495: rho=0, elementary G8, finite G8, projected G2 and G6, each with baseline and trim local rows. L3+G3 without refined rank information is represented by the finite rank-free tail payment, a consequence of the exact quotient and structural trace bound. Adding G4 yields the full route. G7's direct pieces were retained in the full minimization rather than assigned an invented positive density. G9's finite whole-budget bridges give zero guaranteed numerical RHS with the supplied data; their conditional nonzero moment constants remain a separate obligation.

## 7. Enumeration and joint parameter search

The combined ledgers contain 26 active modules. The enumerator topologically traverses all 2^26=67,108,864 subset candidates. Exact dependency pruning removes 67,021,872 and leaves **86,992** legal subsets. Every legal bitmask is retained in ALL_RAW_LEGAL_SUBSETS.json.gz with canonical module order. The standalone replay reconstructs the traversal and compares the complete ordered list and counts.

The payment-view tags restrict additive substitution, not coexistence as factual inequalities. Consequently multiple-payment subsets are included with intersection/maximum semantics. Illegal sum operations are forbidden by the graph and resource ledger; they are not misrepresented as mathematically legal numerical evaluations. There are no unresolved state-identity compatibility obligations between the usable A/B modules.

All exact same-state constraints can legally be augmented to their union, which can only shrink the adversarial feasible set. This is a proved information-dominance reduction, not pruning because a module's standalone gain is small. B05 includes B06, trace-refined G3 dominates the Young family, and G3/G4 imply the projected payments. B08 and a certified plane make the optional G5 schema redundant. These implication proofs are recorded in MODULE_GRAPH.

The resulting two maximal qualified classes are C_EXACT_MAXIMAL and C_CONDITIONAL_MOMENTS. Every raw subset is assigned to one, with counts. The first is the only numerically instantiable non-dominated class and was jointly searched. The second lacks frozen d0,qbar and is recorded UNINSTANTIABLE_WITH_FROZEN_INPUTS; choosing either freely would violate A's ledger. This class is not discarded and has no fabricated gain/no-gain result. The existence of a proven strict gain in the exact class does not require that missing conditional branch to be instantiated.

For each of the 501 integers m=250,...,750, the search optimized rho and the seven linear plane parameters (R and six nonnegative prices) jointly at the frozen comparator. Both local families were scanned, although the baseline family is dominated by the trim family. Candidate vertices supplied the finite floating LP screen. Exact Farkas plane certification independently validates the competitive choices afterward.

The rho range 0<=rho<=4 is justified. In a minimum of the increasing profile-plus-pressure objective, e may be reduced to at most max_j epsilon_j, since the energy coefficient in every row is one and all pressures are nonnegative. Throughout m<=750, (m-6)max epsilon_j <=744 epsilon_c20<tau*9. At rho=4 the profile is consequently linear on this entire minimizing range. Raising rho further leaves the local optimum unchanged while the adverse global penalty is nondecreasing. No maximizing rho>4 is lost. No such energy cap is used by the exact theorem verifier.

At fixed energy the profile is concave in rho. The hypograph of the best LP value is a projection of a convex feasible hypograph; the global adverse penalty is a supremum of affine functions of rho and hence convex. Thus the scalar screened objective is concave, and the bounded scalar search is not a collection of arbitrary starts. Floating precision is used only for discovery; no exact optimality claim depends on its stopping tolerance.

The top trim neighborhood m=486,...,498 was reoptimized at the resulting bound, not just at fixed comparator, and exact-certified. The best exact separator occurs at m=492. All m=495 regression alternatives were also exact-certified. The inherited exact baseline r* is kept as the baseline leaderboard entry; the newly rounded rho=0 regression certificate is deliberately slightly weaker and is not interpreted as a baseline loss.

## 8. Frozen winning parameters and endgame

The fixed parameters are

\[
 m=492,\quad\rho=\frac{19569717577}{100000000000000},\quad
 R=\frac{38219880252651}{10000000000000},
\]
\[
 (t_0,...,t_5)=\left(
 \frac{2444609786511}{25000000000000},
 \frac{28224041240321}{100000000000000},
 \frac{24984358312191}{50000000000000},
 \frac{72302683681231}{100000000000000},
 \frac{90853194486369}{100000000000000},
 \frac{1249217915609}{1250000000000}\right).
\]

The full physical shift cost is

\[
 P_{\rm price}=\frac{47854801908769656526884823}{25000000000000000000000000}.
\]

For h<=x<=s0, p_h(x;rho) is increasing in x. Indeed implicit differentiation gives

\[
 (2p+A_x)p' = \rho^2(2-h)/8-(2+\rho)p.
\]

The bound p<rho^2 N_x/(4A_x), together with
\((2-h)A_x-2(2+\rho)N_x=h(h+\rho)>0\), proves p'>0 when rho>0; rho=0 is constant. This argument also makes the endpoint use rigorous, rather than testing a single proportion numerically.

At the rational s0, the stored rational pbar is positive and satisfies

\[
 \bar p^2+A_{s0}\bar p\ge\rho^2N_{s0}/4.
\]

Since the quadratic is increasing for p>=0, p_h(s0;rho)<=pbar. The exact replay then checks the strict positive margin

\[
 mh-P_{\rm price}-m\bar p-(m-R)s_0
 =\frac{197709610897766003525347778736270024097588737952243174610346641}
 {134990102025867487759198256090264582174639743300000000000000000000000000}>0.
\]

If a joint limiting state had h<=x<=s0, the local plane, B08 and G6 would imply

\[
 x-h\ge\frac{Rx-P_{\rm price}}m-p_h(x;\rho)
 \ge\frac{Rx-P_{\rm price}}m-\bar p,
\]

hence (m-R)x>=mh-P_price-m*pbar>(m-R)s0. Since m-R>0, this contradicts x<=s0. Limiting proportions below h are already excluded by the imported basic inequality. Thus the exact strict separator is proved, with one D+Y budget and one full-Gram tail payment.

## 9. Resource accounting, limitations and replay

NO_DOUBLE_COUNT_LEDGER lists every resource and its spend. All local rows share one E coordinate and one pressure vector; each row's own packet is already capacity-checked by B. The final plane spends one band profile. The all-offset count divides by m once and spends one D+rho*ell and the full physical pressure cost. G6 pays y-rho*v once. Rank constraints and retained q0,Z are feasible-region constraints, never independent added credits. AS01/QM01 bound the whole D+Y and NP01 decomposes Y; none is added to the spent total. Higher moments, unsupported span>6 copies, old-kernel weights and new q-certificates are absent.

The supplied ceilings are not extended beyond their frozen scopes. The present result is not an exhaustive theorem about arbitrary heterogeneous packet designs, nor a new discovery of universal local rows. It is an exhaustive legal-module accounting and a completed bounded search in the exported finite fusion family, followed by exact certification of a strict improvement. No subsequent optimization or critic stage follows this first complete freeze.

From this output directory, the cheap read-only ROOT replay is:

```powershell
& 'C:/Users/user/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' -B -X utf8 './verify_fusion.py'
```

It checks the complete 86,992-subset enumeration, primary coefficient binding, the winning whole-polyhedron rational certificate, strict separator, and final output manifest. `--all` additionally rechecks all 24 supplied plane/endgame certificates and takes only a few seconds. No optimizer, external package, local interval replay or network is needed. The final freeze binds RESULT and all payload files; MANIFEST binds every output except itself. ROOT should bind the manifest and then STOP_FOR_SOL_REVIEW.
